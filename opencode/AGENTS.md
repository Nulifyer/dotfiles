# Global opencode instructions

When running Python scripts or creating Python-based helper tools, prefer `pypy3` when it is available and compatible with the required packages. Use `python` as the fallback when PyPy is unavailable or a package does not support PyPy.
